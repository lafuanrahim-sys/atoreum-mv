import CoverflowArc from "@/components/home/CoverflowArc";

/**
 * Template: pinned horizontal 3D coverflow — 5 square cards arced in
 * perspective, scroll-scrubbed so the "centered" card sweeps from the
 * first to the last as the section holds under the viewport. See
 * CoverflowArc.tsx for the ScrollTrigger/rotation mechanics.
 */
export default function CoverflowHero() {
  return <CoverflowArc />;
}
